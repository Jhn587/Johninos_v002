type SomethingElse @model @auth(rules: [{allow: public}]) {
  id: ID!
}

type OrderStatus @model @auth(rules: [{allow: public}]) {
  id: ID!
  orderId: String
  customerId: String
  storeId: String
  order: AWSJSON
}
 